name: loupe
on:
  pull_request:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
permissions:
  contents: write
  pull-requests: write
jobs:
  review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with: { bun-version: "1.3.14" }
      - run: curl -fsSL https://whip.sh/install.sh | bash
      - uses: anish-inf/loupe@pr-11-testing
        env:
          INFERENCE_API_KEY: ${{ secrets.INFERENCE_API_KEY }}
  chat:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with: { bun-version: "1.3.14" }
      - run: curl -fsSL https://whip.sh/install.sh | bash
      - uses: anish-inf/loupe@pr-11-testing
        env:
          INFERENCE_API_KEY: ${{ secrets.INFERENCE_API_KEY }}
